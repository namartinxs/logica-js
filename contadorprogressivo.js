let resposta = prompt("Insira um numero para a contagem progressiva");
let contador = 0;

while (contador <=resposta){
    console.log(`Contando de zero a ${resposta}.Numero atual ${contador}`)
    contador++
}